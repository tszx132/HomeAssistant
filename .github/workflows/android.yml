name: Build Home Assistant App

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-java@v4
      with:
        java-version: '8'
        distribution: 'temurin'
        
    - name: Setup Android SDK
      run: |
        # 下载兼容 Java 8 的命令行工具
        wget -q https://dl.google.com/android/repository/commandlinetools-linux-8092744_latest.zip
        
        mkdir -p android-sdk
        unzip -q commandlinetools-linux-8092744_latest.zip -d android-sdk/
        
        # 查找 sdkmanager
        SDKMANAGER_PATH=$(find android-sdk -name "sdkmanager" -type f | head -1)
        echo "SDKMANAGER_PATH=$SDKMANAGER_PATH" >> $GITHUB_ENV
        
        # 设置环境变量
        echo "ANDROID_SDK_ROOT=$GITHUB_WORKSPACE/android-sdk" >> $GITHUB_ENV
        SDKMANAGER_DIR=$(dirname "$SDKMANAGER_PATH")
        echo "$SDKMANAGER_DIR" >> $GITHUB_PATH
        
    - name: Install Android components
      run: |
        # 接受许可证
        yes | sdkmanager --sdk_root=$GITHUB_WORKSPACE/android-sdk --licenses
        
        # 安装 Android 组件
        sdkmanager --sdk_root=$GITHUB_WORKSPACE/android-sdk "platform-tools"
        sdkmanager --sdk_root=$GITHUB_WORKSPACE/android-sdk "platforms;android-29"
        sdkmanager --sdk_root=$GITHUB_WORKSPACE/android-sdk "build-tools;29.0.3"
        
        echo "$GITHUB_WORKSPACE/android-sdk/platform-tools" >> $GITHUB_PATH
        
    - uses: actions/setup-node@v4
      with:
        node-version: '16'
        
    - name: Install Cordova and compatible Android platform
      run: |
        # 使用经过验证的稳定版本
        npm install -g cordova@9.0.0
        
        # 清理旧平台
        rm -rf platforms/android plugins
        
        # 添加兼容的 Android 平台
        cordova platform add android@8.1.0
        
    - name: Force compatible Gradle version
      run: |
        echo "🔧 强制使用兼容的 Gradle 版本..."
        cd platforms/android
        
        # 下载并设置 Gradle 7.5（与 Cordova Android 8.1.0 兼容）
        wget -q https://services.gradle.org/distributions/gradle-7.5-bin.zip
        unzip -q gradle-7.5-bin.zip
        ./gradle-7.5/bin/gradle wrapper --gradle-version 7.5
        
        # 验证 Gradle 版本
        ./gradlew --version
        
    - name: Build with compatible Gradle
      run: |
        echo "🏗️ 使用兼容的 Gradle 构建 APK..."
        
        # 使用项目本地的 Gradle wrapper
        cd platforms/android
        ./gradlew build
        
    - name: Find and upload APK
      run: |
        echo "📦 查找生成的 APK 文件..."
        
        # 查找所有 APK 文件
        find platforms/android -name "*.apk" -type f | head -10
        
        # 复制 APK 文件到标准位置以便上传
        mkdir -p dist
        find platforms/android/app/build/outputs -name "*.apk" -exec cp {} dist/ \;
        
        echo "找到的 APK 文件:"
        ls -la dist/ || echo "未找到 APK 文件"
        
    - uses: actions/upload-artifact@v4
      with:
        name: home-assistant-app
        path: dist/*.apk
        retention-days: 30